= render 'shared/admin/page_header', page_name: 'Add New Profile'

= simple_form_for(@profile, url: admin_profile_index_path, html: { class: 'form-horizontal'}) do |f|
  = f.input :phone_number
  = f.input :email
  = f.input :description
  = f.input :recommend
  = f.button :submit
