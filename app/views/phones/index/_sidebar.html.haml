.sidebar
  %h2
    %i Brands
  %ul.list-unstyled.my-list
    - @brands.each do |brand|
      %li
        %h4
          = link_to brand_path(brand) do
            %i.fa.fa-apple{ style: 'color: gray;' }
            = "&nbsp;&nbsp;&nbsp; #{brand.name}(#{brand.phones.count})".html_safe
    %li
      %h4
        %a{:href => "#"} All
  %h2 Find us on Facebook
  %ul.list-unstyled.my-list
    %li Facebook